# flex-dir — 설계 결정 기록

Windows 파일 탐색기 대체 앱. 멀티페인으로 여러 폴더를 동시에 다루는 것이 목적.

이 문서는 착수 전 결정과 **그 근거**를 남긴다. 근거는 대부분
[fast-explorer](https://github.com/solitasroh/fast-explorer) 의 실패에서 나왔다.

---

## 1. 전작이 실패한 이유

fast-explorer 는 C++/Win32 로 3주 만에 v0.8.1 까지 갔고 기능은 Q-Dir 급이었다.
탭, 4-pane, 네비게이션 트리, 필터, 그룹핑, 세션 복원, 자동 업데이트까지 있었다.
그런데도 쓰지 않았다.

측정으로 확인한 사실:

| 사실 | 근거 |
|---|---|
| 기능 부족이 원인이 **아니다** | 위 기능 전부 구현 완료 상태였다 |
| 속도는 **성공했다** | 유일하게 실행 가능한 게이트(100k 열거 벤치)가 있던 항목 |
| 실패한 셋은 게이트가 **없었다** | 잔버그 · 조작감 · 네트워크 경로 |
| 매일 안 썼다 | 버그를 발견할 시간 자체가 쌓이지 않았다 |
| 발견해도 못 고쳤다 | `main-window.cpp` 2,836줄. 15-step 리팩터링 후에도 살아남음 |
| 테스트 28% 인데 버그가 이겼다 | 테스트는 떼어낼 수 있는 주변부에만 붙었다. 버그는 WndProc 중심에 살았다 |
| 뷰 모드가 1개였다 | `LVS_REPORT` 뿐. 속도의 비결인 `LVS_OWNERDATA` 가 아이콘·썸네일 뷰를 막았다 |

**두 문장 요약**

- 실행 가능한 게이트가 있던 항목만 성공했다.
- 고칠 수 없는 코드는 고쳐지지 않는다.

기술 스택 자체는 원인이 아니었다. 다만 WndProc 이 god object 를 구조적으로
유도한다는 점은 사실이고, 그것이 재작성을 정당화한다.

---

## 2. 결정

| # | 결정 | 근거 |
|---|---|---|
| 1 | **완전 백지 재작성** | 코드는 안 가져온다. 옛 레포는 참고자료로 유지 |
| 2 | **C# / WPF (.NET 9)** | MVVM 이 WndProc 싱크홀을 제거한다. 커스텀 컨트롤 경험 있음 |
| 3 | **상주 프로세스** | WPF 의 유일한 진짜 약점이 cold start. single-instance + 창 재사용으로 회피 |
| 4 | **v1 축 = 여러 폴더 왕복** | 기본 탐색기가 못 하는 것 하나. 기능 개수로는 탐색기를 못 이긴다 |
| 5 | **v1 = 탈출구 봉쇄 최소선** | 하나라도 못 하는 게 있으면 탐색기를 열게 되고, 한번 열면 그날은 계속 거기서 한다 |
| 6 | **구조 게이트 2개** | 코드비하인드 금지 + 어셈블리 참조 방향 강제 |
| 7 | **도그푸딩을 실행 가능한 게이트로** | "매일 쓰겠다"는 의지 표명이지 게이트가 아니다 |
| 8 | **자율 실행은 채점 가능한 것만** | 조작감을 판정하는 셸 커맨드는 존재하지 않는다 |

### WPF 를 고른 이유

성능 우려에 대한 답:

- 진짜 병목은 UI 렌더링이 아니라 **I/O 와 shell 호출**이다 (아이콘 추출, 네트워크
  왕복, 대용량 열거). 어떤 스택을 쓰든 같다. 전작에서 이미 풀어본 문제다.
- WPF 는 DirectX 리테인드 모드다. 렌더링은 GPU 가속된다.
- 100k 리스트는 `VirtualizingStackPanel` + `VirtualizationMode=Recycling` +
  데이터 가상화로 처리된다. 단 **가상화를 깨뜨리는 패턴**(ScrollViewer 로 감싸기,
  그룹핑, 가변 행 높이)에 걸리면 참혹해진다. 함정이 있는 것이지 느린 것이 아니다.
- 유일한 실질 약점은 cold start. 상주 프로세스로 다룬다.

덤: 전작에서 속도(`LVS_OWNERDATA`)와 뷰 다양성이 상충했는데, WPF 에서는
`DataTemplate` 교체만으로 뷰 모드가 바뀌고 데이터 가상화는 유지된다. 트레이드오프 자체가 사라진다.

---

## 3. v1 범위

**들어감**

- 2분할 (좌/우)
- 탐색: 주소 입력 · 더블클릭 진입 · 상위 · 뒤로/앞으로
- 파일조작 전체: 복사 · 이동 · 삭제 · 이름변경 · 새 폴더, 페인 간 이동 포함
- shell 우클릭 컨텍스트 메뉴
- 탐색기와 클립보드 호환
- 뷰 모드 복수 + 썸네일 (`IShellItemImageFactory` — shell 캐시 재사용, 자체 캐시 없음)
- 정렬 품질: 자연 정렬(`file2 < file10`) · 폴더 먼저 · 다중 키
- 폴더별 뷰·정렬 기억
- 상주 프로세스 (single instance)
- 사용 로그 (도그푸딩 게이트용)

**빠짐**

네트워크/UNC/NAS (**v2** — 단 아키텍처가 막지 않을 것), 탭, 4분할,
네비게이션 트리, 필터, 그룹핑, 다크모드, 자동 업데이트, 벤치마크 CLI.

> v1 의 판정 기준은 기능 개수가 아니다.
> **하루치 작업 루프에서 탐색기를 열 이유가 0인가.**

---

## 4. 게이트

전작의 교훈: 실행 가능한 게이트가 있던 항목만 살아남았다. 따라서 모든 기준을
셸 커맨드로 판정 가능한 형태로 만든다.

| 게이트 | 기준 | 시점 |
|---|---|---|
| **도그푸딩** | 지난 7일 중 5일 이상 사용. 미달 시 새 기능 차단, 버그 수정만 허용 | **v1 완성 이후** ON |
| **코드비하인드 금지** | `.xaml.cs` 는 `InitializeComponent` 만. 위반 시 빌드 실패 | 처음부터 |
| **참조 방향** | `App → Shell` 직접 참조 시 컴파일 에러 | 처음부터 |
| **cold start** | ≤ 1.5s | 잠정 — 실측 baseline 후 조정 |
| **상주 중 창 표시** | ≤ 100ms | 잠정 |
| **폴더 전환 후 첫 항목** | ≤ 150ms | 잠정 |

성능 수치는 **잠정**이다. 전작은 성급한 숫자로 잘못된 것을 최적화했다.
계측 인프라를 먼저 넣고 baseline 을 본 뒤 확정한다.

---

## 5. 아키텍처

```
Core    순수 모델 · 정렬 · 뷰 상태      (UI/COM 의존 없음, 테스트 과녁)
Shell   COM interop (PIDL · 컨텍스트 메뉴 · 썸네일 · 클립보드)
App     WPF · ViewModel · View
```

참조 방향은 한 방향으로 강제한다. `App` 이 `Shell` 을 직접 부르면 컴파일이 깨진다.

**테스트는 ViewModel 을 겨눈다.** 전작에서 테스트가 코드의 28% 였는데도 버그가
이긴 이유는, 버그가 사는 중심(WndProc)이 테스트 불가능한 물건이었기 때문이다.
WPF 에서 그 중심은 평범한 C# 객체다. 이번엔 TDD 가 버그가 있는 곳에 닿는다.

---

## 6. 착수 순서

```
0. shell 지식 추출 패스   fast-explorer 에서 PIDL·UNC·성능 설계 지식을
                          짧은 문서 하나로 → harness guardrails 에 등록
1. /harness:guard         harness.config.json + 위 게이트 정의
2. 3-어셈블리 뼈대         Core / Shell / App
3. v1 구현                 Core·ViewModel 은 자율 실행 / Shell·UI 는 수동
```

### 0번이 필수인 이유

전작의 shell 지식은 **문서가 아니라 코드에만** 있다. 문서는 전부 프로세스
기록(PDCA · 마일스톤 · handoff)이고, shell 엣지케이스는 `usage-guide.md` 에
한 줄 있는 것이 전부다:

> "Shell link 가 PIDL-only target 이거나 Windows 가 파일시스템/UNC target 을
> 제공하지 않는 경우에는 제한될 수 있습니다."

백지에서 시작하면 이 지식은 자동으로 따라오지 않는다. 그리고 모르는 것은
찾을 수도 없다. 추출해서 guardrails 에 넣으면 모든 구현 세션이 참조하게 된다.
단 guardrails 문서는 매 step 프롬프트에 통째로 복사되므로 **짧고 사실만** 담는다.

### 자율 실행의 경계

| 영역 | 방식 | 이유 |
|---|---|---|
| Core · ViewModel 로직 | `/harness:run` 자율 | 자동 채점 가능 |
| Shell interop | 수동 | 엣지케이스가 실물 검증을 요구 |
| UI 조작감 | 수동 + 매일 사용 | **채점하는 셸 커맨드가 존재하지 않는다** |
