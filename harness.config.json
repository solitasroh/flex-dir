{
  "project": "flex-dir",
  "phasesDir": "phases",
  "agent": "auto",

  "//guardrails": "매 step 프롬프트에 통째로 복사된다. 구현이 실제로 참조할 규칙만 넣는다. PRD·ADR·DESIGN 은 제외하고 필요한 step 지시서에서 경로로 지정해 읽게 한다. DECISIONS.md(실패 부검)는 루트에 두고 넣지 않는다. SHELL_NOTES.md 도 제외 — 자율 실행 대상은 Core·ViewModel(ADR-009)이라 shell 지식이 필요 없고, shell 작업은 수동이라 그때 직접 읽는다. 대신 경로 정규화·열거·오류 매핑·네트워크를 다루는 step 지시서의 '먼저 읽을 것'에 docs/SHELL_NOTES.md 를 반드시 명시할 것.",
  "guardrails": [
    "CLAUDE.md",
    "docs/ARCHITECTURE.md",
    "docs/UI_GUIDE.md"
  ],

  "branchPrefix": "feat-",
  "maxRetries": 3,
  "timeoutSec": 1800,

  "//verify": "솔루션이 아직 없다. /harness:guard 에서 실제로 돌려 시간을 재고 확정한다. check-codebehind 는 ADR-006 의 구조 게이트 스크립트로, 아직 작성되지 않았다.",
  "verify": {
    "fast": [
      "dotnet build --nologo -warnaserror",
      "dotnet test --nologo",
      "pwsh -File scripts/check-codebehind.ps1"
    ],
    "full": [
      "dotnet build -c Release --nologo -warnaserror"
    ]
  },

  "//qualityGate": "솔루션과 스크립트가 생기기 전까지 false. 없는 커맨드로 매 턴을 막으면 아무 일도 못 한다. /harness:guard 에서 켠다.",
  "qualityGate": {
    "enabled": false,
    "group": "fast",
    "timeoutMs": 300000
  },

  "commit": {
    "feat": "feat({phase}): step {num} — {name}",
    "chore": "chore({phase}): step {num} output",
    "done": "chore({phase}): mark phase completed"
  },

  "//tdd": "ADR-012. tdd-guard.mjs 의 LANGS 에 cs 가 없어 .cs 파일은 무조건 통과한다. 켜두면 '켜진 척하는 게이트'가 된다. 플러그인에 cs 지원을 추가한 뒤 enabled 를 true 로, languages 에 cs 를 넣는다.",
  "tdd": {
    "enabled": false,
    "languages": [],
    "verifyRed": true,
    "exclude": []
  },

  "secrets": {
    "extraPatterns": [],
    "allow": []
  },

  "danger": {
    "extraPatterns": []
  }
}
