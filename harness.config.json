{
  "project": "flex-dir",
  "phasesDir": "phases",

  "//agent": "auto 로 두면 안 된다 — 실행기가 codex 를 먼저 고르는데, 이 저장소의 TDD·Danger 가드는 Claude Code 플러그인 훅이고 codex 는 .codex/hooks.json 을 본다. 그 파일이 없으므로 codex 로 돌리면 두 가드가 조용히 사라진다. Quality Gate 는 실행기가 직접 돌려 살아남지만, 테스트 우선 강제가 없어진다.",
  "agent": "claude",

  "//guardrails": "매 step 프롬프트에 통째로 복사된다. 구현이 실제로 참조할 규칙만 넣는다. PRD·ADR·DESIGN 은 제외하고 필요한 step 지시서에서 경로로 지정해 읽게 한다. DECISIONS.md(실패 부검)는 루트에 두고 넣지 않는다. SHELL_NOTES.md 도 제외 — 자율 실행 대상은 Core·ViewModel(ADR-009)이라 shell 지식이 필요 없고, shell 작업은 수동이라 그때 직접 읽는다. 대신 경로 정규화·열거·오류 매핑·네트워크를 다루는 step 지시서의 '먼저 읽을 것'에 docs/SHELL_NOTES.md 를 반드시 명시할 것. ⚠ ARCHITECTURE.md 도 2026-08-24 에 제외했다 — 프롬프트는 argv 로 넘어가고 Windows CreateProcess 의 상한이 32767자인데, 2구간 step 5 에서 32963자로 넘겨 WinError 206 으로 죽었다. guardrails 는 매 step 고정 비용이고 step 요약은 step 당 약 1780자씩 쌓이므로, 가장 큰 파일(9336자)을 빼는 것 말고는 뒤 step 이 전부 넘긴다. 잃는 것이 적은 이유: ARCHITECTURE 의 강제 규칙 셋(코드비하인드·App→Shell 참조·Core 순수성)은 프롬프트가 아니라 scripts/check-structure.ps1 이 기계로 잡는다. 대신 구조를 건드리는 step 지시서의 '읽어야 할 파일'에 docs/ARCHITECTURE.md 를 반드시 명시할 것.",
  "guardrails": [
    "CLAUDE.md",
    "docs/UI_GUIDE.md"
  ],

  "branchPrefix": "feat-",
  "maxRetries": 3,
  "timeoutSec": 1800,

  "//verify": "실측(warm) 합계 9.4s — build 1.4 / test 3.9 / 구조게이트 4.1. 콜드 첫 빌드는 8.3s. check-structure.ps1 은 ADR-006 의 구조 게이트로 코드비하인드·App→Shell 참조·Core 순수성 셋을 본다(참조 방향은 컴파일이 막아주지 않는다 — ProjectReference 를 추가하면 그냥 빌드된다). 한계: 테스트가 0개면 dotnet test 도 exit 0 이므로, 게이트의 테스트 절반은 첫 테스트가 들어온 뒤부터 유효하다. --blame-hang 은 필수다 — 1-core-pipeline step 2 에서 취소 토큰을 넘기지 않은 fake 때문에 반복자 dispose 가 풀리지 않아 dotnet test 가 무한 대기했고, 게이트가 '실패'가 아니라 '매달림'이 되어 실행기까지 교착됐다. hang 은 실패로 만들어야 진단이 가능하다.",
  "verify": {
    "fast": [
      "dotnet build --nologo -warnaserror",
      "dotnet test --nologo --blame-hang --blame-hang-timeout 120s",
      "pwsh -File scripts/check-structure.ps1"
    ],
    "full": [
      "dotnet build -c Release --nologo -warnaserror"
    ]
  },

  "//qualityGate": "실측 후 켰다. false 로 두면 실행기가 verify 를 아예 건너뛰고(execute.py 의 _run_verify) 에이전트가 index.json 에 쓴 completed 를 그대로 믿고 커밋한다 — 전작이 25 step 전부 자가 채점을 통과했던 그 상태다. 끄지 말고 fast 를 줄여라.",
  "qualityGate": {
    "enabled": true,
    "group": "fast",
    "timeoutMs": 300000
  },

  "commit": {
    "feat": "feat({phase}): step {num} — {name}",
    "chore": "chore({phase}): step {num} output",
    "done": "chore({phase}): mark phase completed"
  },

  "//tdd": "ADR-012 완료 — harness 0.2.0 부터 cs 를 안다. 테스트는 <프로젝트>.Tests 하위에서 소스 구조를 미러링한 FooTests.cs 를 찾는다. 생성 코드와 *.xaml.cs 는 검사하지 않는다. red 검증이 콜드 dotnet test 때문에 60초를 넘기기 시작하면 verifyRed 를 false 로 내리고 Quality Gate 에 맡긴다 — 타임아웃은 차단이 아니라 조용한 통과가 되므로 방치하면 안 된다. exclude 의 .harness/probe 는 ADR-015 의 수동 확인용 프로브다 — 검증 도구에 TDD 를 요구하면 테스트를 검증하기 위한 테스트가 된다.",
  "tdd": {
    "enabled": true,
    "languages": ["cs"],
    "verifyRed": true,
    "exclude": [".harness/probe"]
  },

  "secrets": {
    "extraPatterns": [],
    "allow": []
  },

  "danger": {
    "extraPatterns": []
  }
}
