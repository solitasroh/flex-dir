namespace FlexDir.App.Views;

// 코드비하인드에는 로직을 두지 않는다 (CLAUDE.md §2). scripts/check-structure.ps1 이 지킨다.
public sealed partial class MainWindow
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
