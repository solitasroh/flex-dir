# 이 파일을 `.env` 로 복사하고 값을 채운다. `.env` 는 `.gitignore` 가 막으므로
# 저장소에 들어가지 않는다 (이 예시 파일만 커밋된다).
#
# 쓰이는 곳: 제품 아이콘 생성 (scripts/new-icon.ps1).
# 한 번 만들고 나면 .ico 가 저장소에 들어가므로 빌드에는 키가 필요 없다.
# 문서: https://developers.openai.com/api/docs/guides/image-generation

OPENAI_API_KEY=
